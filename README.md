# Playwright TypeScript Sample

Minimal Playwright TypeScript project for Testinium scenario discovery.

## Commands

```bash
npm install
npm run test:list
npm test
```

The tests use inline HTML with `page.setContent`, so they do not require external internet access.
